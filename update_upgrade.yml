---
- name: Update cache and upgrade all packages
  hosts: all
  tasks:
    - name: Update cache and upgrade all packages
    ansible.builtin.apt:
      update_cache: yes
      upgrade: dist  # or 'yes' for a standard upgrade
    become: yes
